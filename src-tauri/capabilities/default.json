{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "main-capability",
  "description": "ModelDock desktop permissions. Ollama lifecycle is controlled by Rust commands, not arbitrary frontend shell access.",
  "windows": ["main"],
  "permissions": [
    "core:default"
  ]
}
