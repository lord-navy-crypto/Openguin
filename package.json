{
  "name": "openguin-preview",
  "private": true,
  "version": "0.10.1",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc -b && vite build",
    "preview": "vite preview",
    "verify:engineering": "python3 scripts/verify-engineering011.py",
    "verify:prepared": "python3 scripts/verify-prepared-backend.py",
    "desktop:prepare": "python3 scripts/apply-build-fixes.py && python3 scripts/apply-full-logs.py && python3 scripts/apply-observatory.py && python3 scripts/apply-performance09.py && python3 scripts/apply-task-center.py && python3 scripts/apply-expansion010.py && python3 scripts/apply-openguin-brand.py && python3 scripts/ensure-app-icon.py && bash scripts/prepare-ollama-sidecar.sh",
    "desktop:dev": "npm run desktop:prepare && npm run verify:engineering && npm run verify:prepared && tauri dev",
    "desktop:build": "npm run desktop:prepare && npm run verify:engineering && npm run verify:prepared && tauri build"
  },
  "dependencies": {
    "@vitejs/plugin-react": "latest",
    "vite": "latest",
    "typescript": "latest",
    "react": "latest",
    "react-dom": "latest",
    "lucide-react": "latest",
    "@tauri-apps/api": "^2.0.0",
    "@tauri-apps/plugin-shell": "^2.0.0"
  },
  "devDependencies": {
    "@types/react": "latest",
    "@types/react-dom": "latest",
    "@tauri-apps/cli": "^2.0.0"
  }
}
